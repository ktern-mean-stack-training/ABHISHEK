To create a new module ng g m <name of the module> --routing.

To create a new component in the module :
    1. navigate to the particular folder and open an integrated terminal there.
    2. so, to create a new component in the module ng g m <name of the component> --module=<name of the module>

Routing the created components is necessary in the routing module.

USING PIPES LIKE DATE PIPE AND CURRENCY PIPE IN THE HOME component

ALONG WITH DEFAULT PIPES, HERE WE HAVE ALSO USED CUSTOM PIPES. ng g g <name of the pipe>
  rasing a power of a value by 2 in home component itself where the name of the pipe is newPipe.


Then the home page is about DIRECTIVES like ngClass and ngStyle.

and then parent child communication
